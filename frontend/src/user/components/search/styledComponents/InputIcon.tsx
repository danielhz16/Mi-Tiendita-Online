import styled from "styled-components";

export const InputIcon = styled.div`
  color: black;
  display: inline-flex;
  align-items: center;
  position: relative;
  .p-inputtext {
    padding-left: 2.5rem; 
    width: 25rem;
  }
  .pi-search {
    position: absolute;
    left: 0.5rem;
    }
  button {
    border: none;
    background-color: transparent;
  }
`